#include <stdio.h>

/**
 * main - prints the name of the current source file
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Use printf function to print the filename */
	printf("%s\n", __FILE__);

	/* Return 0 to indicate success */
	return (0);
}
