#include <stdio.h>
/**
 * Description:main - prints out a string to stdout.
 * Return:0 if success.
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
